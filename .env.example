# Copy this file to `.env.local` and fill the values. Do NOT commit secrets to version control.
# Server-only secrets should remain private (do NOT prefix with `NEXT_PUBLIC_`).

########################
# Required environment variables
########################

# Public (exposed to browser) - Strapi base URLs
NEXT_PUBLIC_STRAPI=https://your-strapi-url.com
NEXT_PUBLIC_STRAPI_API_URL=https://your-strapi-url.com/api

# Strapi auth/token for server-side calls (used as `Authorization` header)
STRAPI_AUTH_API=Bearer YOUR_STRAPI_API_KEY_OR_TOKEN

# NextAuth / Google OAuth (server-side secrets)
NEXT_AUTH_GOOGLE_ID=your-google-client-id.apps.googleusercontent.com
NEXT_AUTH_GOOGLE_SECRET=your-google-client-secret

# NextAuth secret(s) (server-side)
NEXT_AUTH_SECRET=some-long-random-string
AUTH_SECRET=some-long-random-string

# PhonePe (server-side)
PHONEPE_MERCHANT_ID=your_phonepe_merchant_id
PHONEPE_SALT_KEY=your_phonepe_salt_key


########################
# Optional / Recommended (currently hard-coded in code)
########################
# The app currently has Stripe publishable key and Firebase config hard-coded.
# It's recommended to move them to envs like below and update the code to read them.

# Stripe (frontend)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE=pk_test_...

# Stripe (server) - optional if you add server-side Stripe integration
# STRIPE_SECRET=sk_test_...

# Firebase (optional - currently in `src/config/firebase-config.js`)
# FIREBASE_API_KEY=...
# FIREBASE_AUTH_DOMAIN=...
# FIREBASE_PROJECT_ID=...
# FIREBASE_STORAGE_BUCKET=...
# FIREBASE_MESSAGING_SENDER_ID=...
# FIREBASE_APP_ID=...
# FIREBASE_MEASUREMENT_ID=...

# Notes:
# - `NEXT_PUBLIC_*` variables are available to client-side code. Keep only non-sensitive
#   endpoints or publishable keys there.
# - Server-only secrets (Google client secret, auth secrets, PhonePe salt, STRAPI_AUTH_API)
#   must NOT be prefixed with `NEXT_PUBLIC_` and should only be set on the server or
#   in deployment secret management.